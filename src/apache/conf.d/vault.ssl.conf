<VirtualHost *:80>

ServerName vault.yourdomain.com
ServerAlias www.vault.yourdomain.com

RewriteEngine On
RewriteCond %{HTTPS} !=on
RewriteRule ^/?(.*) https://%{SERVER_NAME}/$1 [R,NE,L]

</VirtualHost>


<VirtualHost *:443>

ServerName vault.yourdomain.com
ServerAlias www.vault.yourdomain.com

# Logging
LogLevel warn
ErrorLog /var/log/httpd/vault.yourdomain.com-error_log
CustomLog /var/log/httpd/vault.yourdomain.com-access_log combined

# Reverse proxy configuration
<Location />
ProxyPass http://localhost:10280/
ProxyPassReverse http://localhost:10280/
</Location>

Include /etc/letsencrypt/options-ssl-apache.conf

</VirtualHost>