<VirtualHost *:80>

ServerName dregistryui.yourdomain.com
ServerAlias www.dregistryui.yourdomain.com

RewriteEngine On 
RewriteCond %{HTTPS} !=on 
RewriteRule ^/?(.*) https://%{SERVER_NAME}/$1 [R,NE,L]

</VirtualHost>


<VirtualHost *:443>

ServerName dregistryui.yourdomain.com
ServerAlias www.dregistryui.yourdomain.com

# Logging
LogLevel warn
ErrorLog /var/log/httpd/dregistryui.yourdomain.com-error_log
CustomLog /var/log/httpd/dregistryui.yourdomain.com-access_log combined

# Reverse proxy configuration
<Location />
ProxyPass http://localhost:10580/
ProxyPassReverse http://localhost:10580/
</Location>

Include /etc/letsencrypt/options-ssl-apache.conf

</VirtualHost>
