<VirtualHost *:80>

ServerName dregistry.yourdomain.com
ServerAlias www.dregistry.yourdomain.com

RewriteEngine On 
RewriteCond %{HTTPS} !=on 
RewriteRule ^/?(.*) https://%{SERVER_NAME}/$1 [R,NE,L]

</VirtualHost>


<VirtualHost *:443>

ServerName dregistry.yourdomain.com
ServerAlias www.dregistry.yourdomain.com

# Logging
LogLevel warn
ErrorLog /var/log/httpd/dregistry.yourdomain.com-error_log
CustomLog /var/log/httpd/dregistry.yourdomain.com-access_log combined

# Reverse proxy configuration
<Location />
ProxyPass http://localhost:10480/
ProxyPassReverse http://localhost:10480/
</Location>

Include /etc/letsencrypt/options-ssl-apache.conf

</VirtualHost>
