<VirtualHost *:80>

ServerName project.yourdomain.com
ServerAlias www.project.yourdomain.com

RewriteEngine On
RewriteCond %{HTTPS} !=on
RewriteRule ^/?(.*) https://%{SERVER_NAME}/$1 [R,NE,L]

</VirtualHost>


<VirtualHost *:443>

ServerName project.yourdomain.com
ServerAlias www.project.yourdomain.com

# Logging
LogLevel warn
ErrorLog /var/log/httpd/project.yourdomain.com-error_log
CustomLog /var/log/httpd/project.yourdomain.com-access_log combined

# Reverse proxy configuration
<Location />
ProxyPass http://localhost:10380/
ProxyPassReverse http://localhost:10380/
</Location>

Include /etc/letsencrypt/options-ssl-apache.conf

</VirtualHost>